{
  "name": "@osmoscraft/osmosfeed-core",
  "version": "0.0.1-alpha.0",
  "description": "",
  "type": "module",
  "main": "dist/lib/index.js",
  "types": "index.d.ts",
  "scripts": {
    "start": "ts-node-dev --project tsconfig.json --respawn --watch src/**/*.css --transpile-only src/run-e2e.ts",
    "build": "node scripts/build.js",
    "build:types": "tsc --declaration --emitDeclarationOnly",
    "clean": "rm -rf dist src/__tests__/cache-output src/__tests__/render-output",
    "format": "prettier --write .",
    "test": "ts-node-dev --project tsconfig.json --transpile-only src/run-test.ts",
    "test:watch": "ts-node-dev --project tsconfig.json --respawn --watch --transpile-only src/run-test.ts"
  },
  "author": "osmoscraft",
  "license": "MIT",
  "dependencies": {
    "cheerio": "^1.0.0-rc.10",
    "got": "^11.8.3",
    "htmlparser2": "^7.2.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@osmoscraft/typescript-testing-library": "^0.0.5",
    "@types/node": "^16.11.9",
    "@types/uuid": "^8.3.3",
    "esbuild": "^0.13.15",
    "tiny-glob": "^0.2.9"
  }
}
